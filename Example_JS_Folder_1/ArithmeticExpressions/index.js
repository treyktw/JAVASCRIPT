/* 

arithmetic expression is a combination of 
orperands (values, varibales, etc.)
operator (+ - * / %) // modulus or % give the remainder of any variable
that can be evaluated to a value
ex. y = x + 5

*/

let students = 20;

// students = students - 1;
// students = students + 1;
// students = students * 2;
// students = students / 2;
//let extrastudents = students % 2;

//students += 1;// augmented assignment operator
//students -= 1; 
//students *= 1; 
//students /= 2; 
//! can use if re assigning the same variable

/*

    operator precedence
    1. parenthesis ()
    2. exponents
    3. multiplication & division
    4. addition and subtraction

*/ 
let result = 1 + 2 * (3 + 4)



console.log(students);
//console.log(extrastudents);
console.log(result);

